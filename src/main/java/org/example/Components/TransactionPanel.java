package org.example.Components;

import javax.swing.*;
import java.awt.*;

public class TransactionPanel extends JPanel {
    public TransactionPanel() {
        setBackground(Color.CYAN);
        add(new JLabel("Giao dịch", SwingConstants.CENTER));
    }
}
